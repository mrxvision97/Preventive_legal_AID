# Jetson Nano Optimized Requirements
# Minimal dependencies for ARM64 architecture
# Install with: pip install -r requirements-jetson-nano.txt

# ============================================
# Core FastAPI and Server (Required)
# ============================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# ============================================
# AI Services (Required)
# ============================================
# OpenAI - for AI analysis and OCR (Vision API)
openai==1.3.5

# HTTP Client for Ollama API calls
httpx==0.25.2

# ============================================
# Redis (Optional but Recommended)
# ============================================
# Redis for caching - lightweight and fast
redis==5.0.1

# LangCache - optional Redis caching layer
# Uncomment if you want advanced caching
# langcache==0.1.0

# ============================================
# Image Processing (Required for OCR)
# ============================================
# Pillow - for image processing
Pillow==10.1.0

# Tesseract OCR - optional fallback (requires system Tesseract installed)
# Install Tesseract first: sudo apt-get install tesseract-ocr tesseract-ocr-eng tesseract-ocr-hin
# Then uncomment:
# pytesseract==0.3.10

# ============================================
# Utilities (Required)
# ============================================
# Environment variables
python-dotenv==1.0.0

# Logging
structlog==23.2.0

# Date/time utilities
python-dateutil==2.8.2
pytz==2023.3

# ============================================
# Rate Limiting (Optional)
# ============================================
slowapi==0.1.9

# ============================================
# CORS (Required for frontend)
# ============================================
fastapi-cors==0.0.6

# ============================================
# Optional Packages (Uncomment if needed)
# ============================================

# AWS S3 Storage (only if using cloud storage)
# boto3==1.29.7
# botocore==1.32.7

# Offline Voice Processing (only if using offline transcription/TTS)
# Note: These are HEAVY and may not work well on Jetson Nano 4GB
# openai-whisper==20231117  # Requires PyTorch
# pyttsx3==2.90
# torch==2.1.0  # CPU version - install separately for ARM64
# torchaudio==2.1.0  # CPU version - install separately for ARM64

# LangChain (only if using RAG - currently disabled)
# langchain==0.0.340
# langchain-openai==0.0.2
# langchain-community==0.0.10
# langchain-text-splitters==0.0.1
# tiktoken==0.5.2

# Vector Databases (only if using RAG)
# pinecone-client==5.0.0
# weaviate-client==3.25.3

# PDF Processing (only if processing PDFs)
# PyPDF2==3.0.1
# pdfplumber==0.10.3

# ============================================
# Database Packages (NOT NEEDED - DB disabled)
# ============================================
# sqlalchemy[asyncio]==2.0.23
# asyncpg==0.29.0
# alembic==1.12.1
# psycopg2-binary==2.9.9
# pgvector==0.2.4

# ============================================
# Authentication (NOT NEEDED - public endpoints only)
# ============================================
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4

# ============================================
# Testing (NOT NEEDED for production)
# ============================================
# pytest==7.4.3
# pytest-asyncio==0.21.1
# pytest-cov==4.1.0

